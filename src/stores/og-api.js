import {APIBASE} from '../config'

const LOGIN = APIBASE + '/login'
const AUTH = APIBASE + '/auth'
const PROFILE = APIBASE + '/profile'
const GENERAL = APIBASE + '/general'

export default {
  LOGIN,
  AUTH,
  PROFILE,
  GENERAL
}
